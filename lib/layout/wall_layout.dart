import 'package:flutter/material.dart';

class WallLayout extends StatelessWidget {
  const WallLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
